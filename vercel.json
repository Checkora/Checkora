{
  "version": 2,
  "buildCommand": "python manage.py collectstatic --noinput && (g++ -O2 -o game/engine/main game/engine/main.cpp && chmod +x game/engine/main || echo 'Skipping C++ compile on Vercel; Python fallback will be used')",
  "functions": {
    "api/wsgi.py": {
      "runtime": "python3.12",
      "includeFiles": "game/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/wsgi" }
  ]
}
