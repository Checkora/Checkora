{
  "version": 2,
  "installCommand": "pip install -r requirements.txt",
  "functions": {
    "api/wsgi.py": {
      "runtime": "@vercel/python",
      "includeFiles": ["core/**", "game/**"]
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/wsgi" }
  ]
}