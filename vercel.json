{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt && g++ -O2 -o game/engine/main game/engine/main.cpp && chmod +x game/engine/main",
  "outputDirectory": "public",
  "functions": {
    "api/wsgi.py": {
      "runtime": "@vercel/python",
      "includeFiles": "**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/wsgi" }
  ]
}
