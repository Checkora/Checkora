{
  "version": 2,
  "installCommand": "echo 'Python deps handled by runtime'",
  "buildCommand": "npm install && npm run build && python manage.py collectstatic --noinput && g++ -O2 -o game/engine/main game/engine/main.cpp && chmod +x game/engine/main && mkdir -p public && echo '<!-- -->' > public/placeholder.html",
  "outputDirectory": "public",
  "functions": {
    "api/wsgi.py": {
      "includeFiles": "game/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/wsgi" }
  ]
}
